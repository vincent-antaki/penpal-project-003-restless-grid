[project]
name = "003-squiggly-square"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "core",
    "ipykernel>=7.2.0",
    "pydantic>=2.12.5",
]

[tool.uv.sources]
core = { path = "../../core" }
